

function HomeCover({ name }) {
	useEffect(() => {
		colors[setColor(Math.random(colors.length))]
	}, [])

	return (
		<section
			className={`flex items-end space-x-7 bg-gradient-to-b to-black from-gray-500 h-80 text-white pl-5 pb-5 border-[.5px]`}>			
		</section>
	)
}

export default HomeCover
